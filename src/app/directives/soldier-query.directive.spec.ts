import { SoldierQueryDirective } from './soldier-query.directive';

describe('SoldierQueryDirective', () => {
  it('should create an instance', () => {
    const directive = new SoldierQueryDirective();
    expect(directive).toBeTruthy();
  });
});
